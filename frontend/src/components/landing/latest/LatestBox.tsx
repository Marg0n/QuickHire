import { CardBoxProps } from '@/types/types';
import React from 'react';

const LatestBox = ({
  icon,
  type,
  title,
  platform,
  address,
  tags,
  description,
  className,
}: CardBoxProps) => {
    return (
        <div>
            
        </div>
    );
};

export default LatestBox;